generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Domain models will be added in Phase 1 — Domain Foundation.
// See docs/ERD.md and docs/DOMAIN_MODEL.md.
